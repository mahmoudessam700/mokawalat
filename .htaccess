
# DO NOT REMOVE. CLOUDLINUX DEPLOYMENT CONFIGURATION.
# The following directives are required for the Node.js application to work correctly.

DirectoryIndex disabled
RewriteEngine On
RewriteRule ^$ http://127.0.0.1:%{ENV:PORT}/ [P,L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ http://127.0.0.1:%{ENV:PORT}/$1 [P,L]
