// Deprecated: This script has been intentionally disabled to prevent inserting mock/demo data.
// The application should operate only on real user-provided data.
// If you need migration or backfill utilities, create scoped, reviewed scripts instead.
console.error('scripts/seed.mjs is disabled and will not run. Remove any calls to it.');
process.exit(1);
