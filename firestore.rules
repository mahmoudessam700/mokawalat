rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for all authenticated users during development
    // This is insecure for production. You should implement more granular
    // rules before deploying your app.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
